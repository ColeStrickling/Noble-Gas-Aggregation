# WHAM inputs

Add WHAM metadata files and command examples here.

Recommended organization:

```text
two_body/
three_body/
four_body/
```

Each directory should include:

- `metadata.dat`
- individual `COLVAR` files or processed coordinate/bias files
- the WHAM command used
- the raw WHAM output
- the Jacobian-corrected PMF output
